---
name: digest
description: Свежий сравнительный дайджест новостей об искусственном интеллекте
user-invocable: true
---

# Дайджест новостей об ИИ

## Задача

Найти не первые попавшиеся публикации, а самые важные события точного периода:
новые модели, открытые веса, API, цены, лицензии, независимые измерения и
регулирование. Дайджест должен отвечать на вопрос «что изменилось для человека,
который выбирает или использует AI-модели?».

Не пиши вступления о процессе вроде «соберу из того, что удалось найти».
Первое сообщение пользователю — сразу готовый дайджест.

Пиши естественным русским языком. Не переводи рекламные формулировки дословно,
не склеивай несколько анонсов в одну «суперновость» и не заполняй текст общими
словами вроде «две стороны одной медали». Один пункт — одно событие.

## Свежесть — жёсткий фильтр

1. Через `session_status` узнай текущую дату. По умолчанию период — с 00:00
   семь календарных дней назад до текущего момента. Покажи обе границы в ответе.
2. Для каждого кандидата установи **дату события**, а не дату обновления страницы.
   Релиз месячной давности не становится свежим из-за новой статьи о нём.
3. Событие вне периода исключай, даже если оно кажется важным. Не заполняй им
   пустое место: две свежие новости лучше пяти старых.
4. Если точная дата не подтверждается результатом поиска или страницей, новость
   не включай. Знания модели не считаются подтверждением актуальности.
5. Слова «первая», «лучшая», «обходит», «нашла уязвимость» и численные результаты
   допустимы только со ссылкой на источник, где прямо есть это утверждение.
   Маркетинговый benchmark разработчика обозначай как заявление разработчика.
6. Не используй малоизвестный benchmark как главный аргумент, если не удалось
   понять его методику и найти независимое подтверждение. Лучше честно написать:
   «независимых замеров пока нет».

## Поиск: сначала покрытие, потом ранжирование

1. Достань реестр источников через `memory_search` по запросу «реестр источников,
   AI-дайджест, приоритет доменов».
2. Выполни **три обязательных** `web_search`. Для каждого передай
   `topic="news"`, `searchDepth="advanced"`, `count=8` и точные
   `startDate`/`endDate` в формате `YYYY-MM-DD`:
   - **глобальные релизы:** OpenAI, Anthropic, Google/Gemini, Meta, xAI,
     Mistral; модели, API, цены и доступность;
   - **Азия и open weights:** Qwen/Alibaba, DeepSeek, Kimi/Moonshot,
     MiniMax, GLM/Z.ai; релизы, веса, лицензии и model cards;
   - **проверка рынка:** независимые benchmark, изменения цены, крупные
     сделки и регулирование за тот же период.
   В текст запроса также подставляй границы периода и год. Не полагайся на
   слово `latest`: оно часто возвращает старые релизы.
3. После трёх запросов проверь покрытие: есть ли кандидаты из западного,
   азиатского/open-weight и независимого треков; у каждого ли есть дата события.
   Если один трек пуст или очевидный свежий кандидат упомянут без даты,
   разрешён **один ремонтный `web_search`** по отсутствующему треку или точным
   именам. Не делай отдельный запрос на каждую компанию.
4. Не начинай с отдельного поиска по OpenAI или Anthropic. Порядок компаний в
   реестре источников не является рейтингом важности.
5. `web_fetch` используй максимум для двух первоисточников выбранных кандидатов,
   по одной попытке на URL. Ошибка страницы не отменяет пригодный поисковый
   сниппет, но это нужно честно обозначить.

Перед написанием ответа мысленно составь список кандидатов:

`событие | дата события | первоисточник | независимое подтверждение | влияние`

Сначала примени фильтр даты, затем ранжируй оставшееся.

## Что ставить первым

Главная новость — событие с наибольшим практическим влиянием, а не самый известный
бренд. При прочих равных порядок такой:

1. новая frontier-модель или открытие весов сильной модели;
2. заметное изменение качества, цены, контекста, tool use или доступности API;
3. независимое сравнение, меняющее выбор модели;
4. крупное регулирование или сделка;
5. инфраструктурный анонс без нового пользовательского продукта.

Новый флагман Qwen, DeepSeek, Kimi, MiniMax или GLM не должен проигрывать старой
новости OpenAI/Anthropic только из-за узнаваемости бренда.

## Сравнения

Для главной новости добавь короткое сравнение с одним-двумя актуальными
конкурентами по действительно сопоставимым признакам:

- статус: preview/GA, API/open weights;
- контекст, модальности и tool use;
- официальная цена;
- один и тот же независимый benchmark, если он уже есть.

Не смешивай разные benchmark и не выдавай таблицу разработчика за независимое
измерение. Если независимых результатов нет, напиши именно это.

## Формат для Telegram

Два-три события, 1800–2400 символов. Это обзор на две минуты, а не стена текста.
Без таблиц, `#`-заголовков и голых URL. Ссылки прячь в понятные подписи.
Каждый абзац — не больше трёх коротких предложений.

```text
📰 **ИИ за неделю**
_3–10 августа · 2 минуты_

**За 30 секунд:** один честный вывод недели без рекламных эпитетов.

🔥 **Главное — название события**
`2–4 коротких факта`: размер · доступность · цена/лицензия
**Суть:** одно-два конкретных предложения.
**Кому важно:** практическое следствие для пользователя или разработчика.
**Проверка:** короткое сопоставимое сравнение или честная фраза о том,
что независимых замеров пока нет.
📎 [Анонс](URL) · [Независимая проверка](URL)

⚡ **Ещё важное**

**2. Короткий заголовок**
**Суть:** что изменилось — максимум одно предложение.
**Зачем следить:** кому и почему это пригодится.
📎 [Источник](URL)

**3. Короткий заголовок**
...

🧭 **Итог недели:** один вывод о том, как меняется рынок или выбор моделей.

📗 **Разобрать глубже?** Ответьте номером `1`, `2` или `3` — сравню событие
с конкурентами и объясню без маркетинга.
```

Если независимого источника нет, не рисуй вторую ссылку. Пометку
«заявление разработчика» ставь рядом с конкретным утверждением, а не отдельным
дисклеймером в конце.

Перед отправкой вычитай текст как редактор:

- убери повторы названия компании и канцелярит;
- разбей предложение длиннее 25 слов;
- проверь, что все числа относятся именно к описываемому событию;
- удали сравнение, если модели, режимы или benchmark несопоставимы;
- если событий только два, не выдумывай третье и предложи ответить `1` или `2`.

## Если поиск не работает

Не выдумывай новости из памяти модели и не рассказывай пользователю про
провайдеры, капчи или внутренние ошибки. Скажи: «Сейчас не могу надёжно
проверить свежие новости за период …. Попробуйте ещё раз через несколько
минут».
