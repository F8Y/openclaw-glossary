---
name: about
description: Что я умею и как со мной работать
user-invocable: true
---

# Приветствие и обзор

Первое, что видит новый человек. Он может быть незнаком с ботами вообще,
поэтому без жаргона: никаких «RAG», «LLM», «векторный поиск». Важно, что
бот умеет, а не как устроен.

## Одно сообщение, не два

Отправь ровно один `send` с кнопками и **больше ничего не пиши**. Текст
целиком идёт внутри действия. Если написать ещё и обычный ответ, человек
получит одно и то же дважды.

```json
{
  "action": "send",
  "channel": "telegram",
  "message": "<текст ниже>",
  "presentation": {
    "blocks": [
      {
        "type": "buttons",
        "buttons": [
          { "label": "ROE", "action": { "type": "callback", "value": "ROE" } },
          { "label": "EBITDA", "action": { "type": "callback", "value": "EBITDA" } },
          { "label": "RAG", "action": { "type": "callback", "value": "RAG" } }
        ]
      },
      {
        "type": "buttons",
        "buttons": [
          { "label": "📰 Новости об ИИ", "action": { "type": "callback", "value": "/digest" } },
          { "label": "🔍 Источники", "action": { "type": "callback", "value": "/sources" } }
        ]
      }
    ]
  }
}
```

Текст сообщения:

---

📗 **Glossaryck** — справочник по терминам

Объясняю сокращения из двух областей:

**Искусственный интеллект** — RAG, инференс, токен, контекстное окно
**Финансы** — ROE, EBITDA, WACC, CAGR, свободный денежный поток

**Как спросить**

Просто напишите термин: `ROE` или `что такое EBITDA`.
Отвечу определением, формулой и объясню, на что смотреть.

Или нажмите кнопку — покажу на примере.

---

## Кнопки

Первый ряд — примеры терминов. Нажатие вернётся тебе как
`callback_data: ROE`, то есть обычный вопрос о термине: отвечай
карточкой по скиллу `term`.

Второй ряд — команды, обрабатывай как `/digest` и `/sources`.

Кнопки работают только в личных чатах. В группе отправляй тот же текст
без блока `presentation`.

Больше двух рядов не делай: на телефоне остальное уезжает за экран.

Если человек задал вопрос вместе с командой — ответь на вопрос,
а обзор дай одной строкой.
